<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.0.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

	<context:property-placeholder location="classpath:application.properties"/>

	<!--  
	<jdbc:embedded-database id="dataSource" type="H2">
		<jdbc:script location="classpath:database/security-group-schema.sql" />
		<jdbc:script location="classpath:database/test-group-data.sql" />
	</jdbc:embedded-database>
	-->
	
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
		<property name="driverClassName" value="${database.driver}"/>
		<property name="url" value="${database.url}" />
		<property name="username" value="${database.username}"/>
		<property name="password" value="${database.password}"/>
	</bean>
	
	<!-- UserDetails 관련 SQL확장가능 -->
	<!-- <bean id="jdbcUserService" class="org.springframework.security.provisioning.JdbcUserDetailsManager">
		<property name="dataSource" ref="dataSource"/>
		<property name="authenticationManager" ref="authenticationManager"/>
		<property name="changePasswordSql" value="UPDATE USERS SET PASSWORD = ? WHERE USERNAME = ?"/>
	</bean> -->
	
	<!-- <bean id="userService" class="seungbeomi.spring.security.InMemoryChangePasswordDaoImpl">
		<property name="userProperties">
			<props>
				<prop key="admin">admin, ROLE_ADMIN</prop>
				<prop key="user">user, ROLE_USER</prop>
			</props>
		</property>
	</bean> -->
	
	<!-- TokenBasedRememberMeServices 클래스의 확장 -->
	<!-- <bean id="rememberMeServices" class="seungbeomi.spring.security.IPTokenBasedRememberMeServices">
		<property name="key" value="security"/>
		<property name="userDetailsService" ref="userService"/>
		<property name="parameter" value="_remember_me"/>
		<property name="cookieName" value="REMEMBER_ME"/>
	</bean> -->

	<!--  
	<bean id="accessDecisionManager" class="org.springframework.security.access.vote.AffirmativeBased">
        <property name="decisionVoters">
            <list>
                <ref local="roleVoter"/>
                <ref local="authenticatedVoter"/>
            </list>
        </property>
    </bean>
    <bean id="roleVoter" class="org.springframework.security.access.vote.RoleVoter" />
    <bean id="authenticatedVoter" class="org.springframework.security.access.vote.AuthenticatedVoter" />
	-->

</beans>
